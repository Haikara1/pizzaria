import { useLayoutEffect, useRef } from 'react'
import { gsap } from '../../utils/gsap'
import styles from './Hero.module.css'
export default function Hero(){const root=useRef(null),image=useRef(null),overlay=useRef(null),content=useRef(null);useLayoutEffect(()=>{const ctx=gsap.context(()=>{gsap.matchMedia().add('(prefers-reduced-motion: no-preference)',()=>{gsap.timeline({scrollTrigger:{trigger:root.current,start:'top top',end:'bottom top',scrub:true}}).to(image.current,{scale:1.06,yPercent:5,ease:'none'},0).to(overlay.current,{opacity:1.18,ease:'none'},0).to(content.current,{y:-45,opacity:.12,ease:'none'},0)})},root);return()=>ctx.revert()},[]);return <section ref={root} className={styles.hero} id="inicio" aria-labelledby="hero-title"><img ref={image} src="/images/hero-pizza.png" alt="Pizza artesanal quadrada próxima ao forno a lenha"/><div ref={overlay} className={styles.overlay}/><div ref={content} className={`container ${styles.content}`}><p>Pizza Quadrada</p><h1 id="hero-title">UM NOVO JEITO<br/>DE COMER PIZZA.</h1><a href="#a-casa">Conheça a Quadrada <span>→</span></a></div><a href="#a-casa" className={styles.scroll}>Scroll <span>↓</span></a></section>}
