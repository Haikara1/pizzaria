import { useLayoutEffect,useRef } from 'react'
import { gsap } from '../../utils/gsap'
import styles from './FinalCTA.module.css'
export default function FinalCTA(){const root=useRef(null),image=useRef(null),eyebrow=useRef(null),line=useRef(null),quadrada=useRef(null),button=useRef(null);useLayoutEffect(()=>{const ctx=gsap.context(()=>{gsap.matchMedia().add('(prefers-reduced-motion: no-preference)',()=>{gsap.timeline({scrollTrigger:{trigger:root.current,start:'top 78%',once:true}}).fromTo(image.current,{scale:1.08},{scale:1,duration:1.5,ease:'power2.out'},0).from(eyebrow.current,{opacity:0,y:25,duration:.7,ease:'power3.out'},.1).from(line.current,{opacity:0,y:50,duration:.9,ease:'power3.out'},.2).from(quadrada.current,{opacity:0,y:35,duration:.75,ease:'power3.out'},.48).from(button.current,{opacity:0,y:25,duration:.65,ease:'power3.out'},.7)})},root);return()=>ctx.revert()},[]);return <section ref={root} className={styles.cta} id="pedir"><img ref={image} src="/images/hero-pizza.png" alt="Pizza quadrada pronta para servir"/><div/><section><p ref={eyebrow} className="eyebrow">Seu momento</p><h2><span ref={line}>HOJE É DIA<br/>DE </span><i ref={quadrada}>QUADRADA.</i></h2>{/* TODO: adicionar WhatsApp oficial. */}<a ref={button} href="#">Pedir agora <span>→</span></a></section></section>}
