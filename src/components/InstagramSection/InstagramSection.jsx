import { useLayoutEffect,useRef } from 'react'
import { gsap } from '../../utils/gsap'
import styles from './InstagramSection.module.css'
const posts=['gallery-03.png','about-dough.png','hero-pizza.png','oven-pizza.png','menu-pizzas.png','gallery-04.png']
export default function InstagramSection(){const root=useRef(null),title=useRef(null),grid=useRef(null);useLayoutEffect(()=>{const ctx=gsap.context(()=>{gsap.matchMedia().add('(prefers-reduced-motion: no-preference)',()=>{gsap.from(title.current,{y:50,opacity:0,duration:1,ease:'power3.out',scrollTrigger:{trigger:title.current,start:'top 82%',once:true}});gsap.from(grid.current.children,{y:40,opacity:0,duration:.8,stagger:.1,ease:'power3.out',scrollTrigger:{trigger:grid.current,start:'top 85%',once:true}})})},root);return()=>ctx.revert()},[]);return <section ref={root} className={styles.instagram}><div className="container"><p className="eyebrow">Instagram</p><div className={styles.head}><h2 ref={title}>ACOMPANHE<br/>A QUADRADA</h2><a href="https://www.instagram.com/pizza.quadrada__/" target="_blank" rel="noreferrer">@pizza.quadrada__ ↗</a></div><div ref={grid} className={styles.grid}>{posts.map((name,i)=><a href="https://www.instagram.com/pizza.quadrada__/" target="_blank" rel="noreferrer" key={`${name}-${i}`}><img src={`/images/${name}`} alt={`Publicação demonstrativa ${i+1} da Pizza Quadrada`}/><span>Ver no Instagram ↗</span></a>)}</div></div></section>}
